//const parserTest = require('./parser')

test("Tests FindNotAllowed Function" , () => { //==============================================================

    // Tests (IMPORT PROBLEM)
    // parserTest.matchOpeningTags("<parse ", 0).toBe(5)
    // parserTest.matchOpeningTags("<parse ", -5).toBe(0)
    // parserTest.matchOpeningTags("<parse ", 1).toBe(6)

});
//======================================================================================================================
